openapi: 3.0.0
info: {title: Legacy Patient API v2, version: '2.0'}
paths:
  /patients/{patient_id}:
    get:
      parameters:
      - in: path
        name: patient_id
        required: true
        schema: {type: string}
      responses: {'200': {description: OK}}
